import Tasks from "@/components/Tasks";

// dashboard page main route
const page = () => {
  return (
    <div className="bg-gradient-to-tr from-slate-700 to-slate-900">
      <Tasks />
    </div>
  );
};

export default page;
